import{schedule}from'@netlify/functions';const handler=async()=>{const base=process.env.APP_BASE_URL;if(!base)return new Response('APP_BASE_URL missing',{status:500});for(const provider of['facebook','youtube'])await fetch(`${base}/.netlify/functions/sync-provider`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({provider})}).catch(()=>null);await fetch(`${base}/.netlify/functions/strategy-evaluate`,{method:'POST'}).catch(()=>null);return new Response('ok')};export default schedule('0 1,14 * * *',handler);