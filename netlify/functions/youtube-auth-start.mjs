import{json,err}from'./lib/http.mjs';import{issue}from'./lib/oauth.mjs';export default async()=>{try{if(!process.env.GOOGLE_CLIENT_ID||!process.env.GOOGLE_REDIRECT_URI)return err('Google OAuth chưa cấu hình',500);const state=await issue('youtube'),p=new URLSearchParams({client_id:process.env.GOOGLE_CLIENT_ID,redirect_uri:process.env.GOOGLE_REDIRECT_URI,response_type:'code',access_type:'offline',prompt:'consent',include_granted_scopes:'true',state,scope:'https://www.googleapis.com/auth/youtube.readonly https://www.googleapis.com/auth/yt-analytics.readonly'});return json({authorizationUrl:`https://accounts.google.com/o/oauth2/v2/auth?${p}`})}catch(e){return err(e.message,500)}};