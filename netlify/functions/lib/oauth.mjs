import{randomState}from'./crypto.mjs';import{sb}from'./supabase.mjs';export async function issue(provider){const state=randomState();await sb('oauth_states',{method:'POST',body:{provider,state,expires_at:new Date(Date.now()+600000).toISOString()}});return state}export async function consume(provider,state){const r=await sb(`oauth_states?provider=eq.${provider}&state=eq.${encodeURIComponent(state)}&select=*`),x=r?.[0];if(!x||new Date(x.expires_at)<new Date())throw new Error('OAuth state không hợp lệ');await sb(`oauth_states?id=eq.${x.id}`,{method:'DELETE'})}